# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon

# Xcode — user-specific
xcuserdata/
*.xcuserstate
*.xcscmblueprint
*.xccheckout

# Xcode — build
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Swift Package Manager
.build/
.swiftpm/
# Package.resolved IS tracked: this is an app, so pin exact dependency versions
# for reproducible CI/builds (libraries would omit it).

# CocoaPods / Carthage (falls je genutzt)
Pods/
Carthage/Build/

# Whisper-Modelle (zur Laufzeit geladen, nicht ins Repo)
*.mlmodelc
Models/*.bin
Models/*.gguf

# DMG output
dist/

# Local working notes — kept out of the public repo. FEATURES.md feeds the
# README/website copy; DEFERRED.md is parked work that belongs in GitHub Issues.
FEATURES.md
DEFERRED.md
